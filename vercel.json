{
  "version": 2,
  "buildCommand": "pip install -r requirements.txt",
  "env": {
    "UV_LINK_MODE": "copy"
  },
  "functions": {
    "api/index.py": {
      "maxDuration": 60
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.py" }
  ]
}
